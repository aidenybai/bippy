import type { ReactNode } from "react";
export declare const ErrorBoundary: (props: { fallback?: ReactNode; children?: ReactNode }) => ReactNode;
export declare const init: (options: { dsn: string }) => void;
