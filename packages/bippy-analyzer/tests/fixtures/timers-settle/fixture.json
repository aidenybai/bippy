{
  "entry": "src/main.tsx",
  "expectedStatus": "exact",
  "minCoverage": 1,
  "notes": "The snapshot is taken after a quiet window: a slow interval has not ticked, a chain of effects each scheduling the next short timer runs to its end, and an unimplemented declared host constructor does not invent a throw that suppresses a later timer."
}
