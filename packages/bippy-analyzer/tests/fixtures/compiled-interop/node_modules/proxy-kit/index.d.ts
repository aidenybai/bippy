import type { ReactNode } from "react";
export declare const SessionProvider: (props: { session: string | null; children?: ReactNode }) => JSX.Element;
export declare const useSession: () => { data: string | null; status: string };
export declare const api: {
  Provider: (props: { children?: ReactNode }) => JSX.Element;
  greeting: { useLabel: () => string };
};
