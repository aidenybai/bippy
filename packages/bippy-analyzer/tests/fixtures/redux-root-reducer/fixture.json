{
  "expectedStatus": "exact",
  "minCoverage": 1,
  "expectedStats": { "branchCount": 0, "unknownCount": 0 },
  "expectedStates": 1,
  "externalPackages": ["react-redux"],
  "notes": "`createStore(rootReducer, enhancer)` where the root reducer is a hand-written function around `combineReducers`: the store's slice keys are discovered by running the reducer on `@@redux/INIT` (so `combination` must run each slice reducer and keep the previous state's identity when nothing changed), and the function in second position is the enhancer, not preloaded state.",
  "observations": {
    "globals": {},
    "queries": [],
    "stores": [
      {
        "customization": {
          "isDarkMode": false,
          "fontFamily": "Inter",
          "opened": true
        },
        "auth": {
          "isAuthenticated": false,
          "user": null
        }
      }
    ]
  }
}
