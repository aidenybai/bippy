import type { ReactNode } from "react";
export declare const Stack: (props: { gap: number; children?: ReactNode }) => ReactNode;
export declare const Card: (props: { title: string; children?: ReactNode }) => ReactNode;
