import type { ComponentType } from "react";
export interface TranslateProps {
  t: (key: string) => string;
}
export declare const translate: () => <Props extends TranslateProps>(
  Wrapped: ComponentType<Props>,
) => ComponentType<Omit<Props, "t">>;
