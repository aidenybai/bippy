import type { ReactNode } from "react";
export declare enum VisualState {
  animatingIn = "animating-in",
  showing = "showing",
  hidden = "hidden",
}
export declare enum Density {
  compact = 0,
  roomy = 1,
}
export declare const Sheet: (props: { children?: ReactNode }) => JSX.Element;
