import type { JSX } from "react";

export interface SparklineProps {
  points: number[];
  label?: string;
}

export declare const palette: string[];

declare const Sparkline: (props: SparklineProps) => JSX.Element;
export default Sparkline;
