export declare class Stack<T> {
  constructor(items: T[]);
  push(item: T): Stack<T>;
  toArray(): T[];
}
export declare const createRegistry: <S>(
  reducer: (state: S | undefined, action: { type: string }) => S,
) => { getState: () => S };
export declare const version: string;
