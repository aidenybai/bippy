declare function pathToPattern(path: string): RegExp;
declare namespace pathToPattern {
  function parse(path: string): string[];
  function compile(path: string): (params: Record<string, string | number>) => string;
}
export = pathToPattern;
