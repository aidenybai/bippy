import type { JSX } from "react";

export interface TopBarProps {
  color?: string;
  height?: number;
}

export declare function useTopBar(): { start: () => void; done: () => void };

declare const TopBar: (props: TopBarProps) => JSX.Element;
export default TopBar;
