import type { ReactNode } from "react";

export interface Control {
  _defaultValues: Record<string, unknown>;
}

export interface UseFormReturn {
  control: Control;
  getValues: () => Record<string, unknown>;
}

export declare const useForm: (props?: { defaultValues?: Record<string, unknown> }) => UseFormReturn;
export declare const useFormContext: () => UseFormReturn | null;
export declare const FormProvider: (props: UseFormReturn & { children: ReactNode }) => ReactNode;
