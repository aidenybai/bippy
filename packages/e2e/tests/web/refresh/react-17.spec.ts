// The same ported ReactFresh suite against React 17 through the
// refresh-app-react17 fixture (legacy ReactDOM.render roots and
// react-dom/test-utils act).
import { defineRefreshSuite } from "./refresh-suite";

defineRefreshSuite(17);
