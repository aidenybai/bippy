// The same ported ReactFresh suite against React 18.3 through the
// refresh-app-react18 fixture (aliased react/react-dom, unstable_act).
import { defineRefreshSuite } from "./refresh-suite";

defineRefreshSuite(18);
