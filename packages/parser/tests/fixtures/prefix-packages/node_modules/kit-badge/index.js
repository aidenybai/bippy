import { jsx, jsxs } from "react/jsx-runtime";
import { describeTheme } from "kit-theme";

export const Badge = ({ label }) =>
  jsxs("span", { className: "badge", children: [label, " ", describeTheme()] });
