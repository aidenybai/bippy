{
  "entry": "src/main.tsx",
  "expectedStatus": "exact",
  "minCoverage": 1,
  "expectedStates": 1,
  "notes": "The fixture's vite.config.ts rewrites index.html through transformIndexHtml hooks (pre-ordered object hook returning html + tags, plain function hook); the static side serves the same transformed shell, so document.baseURI, document.title and head queries read what the dev server's page has."
}
