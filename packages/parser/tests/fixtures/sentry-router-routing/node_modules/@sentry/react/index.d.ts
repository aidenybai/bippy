import type { ComponentType } from "react";
export interface RouterTracingOptions {
  useEffect: unknown;
  useLocation: unknown;
  useNavigationType: unknown;
  createRoutesFromChildren: unknown;
  matchRoutes: unknown;
}
export interface Integration {
  name: string;
  setup(): void;
}
export declare const reactRouterV6BrowserTracingIntegration: (options: RouterTracingOptions) => Integration;
export declare const init: (options: { dsn: string; integrations?: Integration[] }) => void;
export declare const withSentryReactRouterV6Routing: <P>(routes: ComponentType<P>) => ComponentType<P>;
