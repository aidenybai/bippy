{
  "entry": "src/main.tsx",
  "expectedStatus": "partial",
  "minCoverage": 1,
  "expectedStats": { "unknownCount": 0, "branchCount": 1 },
  "notes": "Radix ScrollArea's hover scrollbar: an effect listens for pointerenter on the root node. Once a navigation commits, headless Chromium hit-tests the pointer position (the viewport origin) and dispatches the pointer boundary events to whatever element lies there, so the handler may run before the snapshot when the root reaches the origin; without layout that is a residual branch, hidden here because the body margin keeps the root away from the origin."
}
