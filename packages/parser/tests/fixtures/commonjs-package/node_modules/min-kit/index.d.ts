import type { ReactNode } from "react";
declare const Chip: ((props: { tone?: string; children?: ReactNode }) => JSX.Element) & {
  defaultProps: { tone: string };
};
export default Chip;
export declare const tones: string[];
export declare const chipCount: number;
