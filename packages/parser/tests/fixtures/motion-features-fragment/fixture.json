{
  "entry": "src/main.tsx",
  "expectedStatus": "exact",
  "minCoverage": 1,
  "notes": "framer-motion 4 renders a motion component as a fragment of `MotionContext.Provider` and the keyed array of enabled renderless feature components (`useFeatures`), an empty fragment when no feature prop is set. The package is a pared-down copy of `createMotionComponent` and `featureDefinitions` at 4.1.17."
}
