"use strict";

var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
var React = require("react");
var shim_1 = require("use-sync-external-store/shim");
var with_selector_1 = require("use-sync-external-store/shim/with-selector");
var store_1 = require("./store.js");

var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element");
var hasOwnProperty = Object.prototype.hasOwnProperty;
var RESERVED_PROPS = { key: true, ref: true, __self: true, __source: true };

function ReactElement(type, key, ref, props) {
    return {
        $$typeof: REACT_ELEMENT_TYPE,
        type: type,
        key: key,
        ref: ref,
        props: props,
        _owner: null
    };
}
function jsx(type, config, maybeKey) {
    var propName;
    var props = {};
    var key = null;
    var ref = null;
    if (maybeKey !== undefined) key = "" + maybeKey;
    if (config.key !== undefined) key = "" + config.key;
    for (propName in config) {
        if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
            props[propName] = config[propName];
        }
    }
    if (type && type.defaultProps) {
        var defaultProps = type.defaultProps;
        for (propName in defaultProps) {
            if (props[propName] === undefined) props[propName] = defaultProps[propName];
        }
    }
    return ReactElement(type, key, ref, props);
}

exports.ThemeContext = React.createContext("light");
exports.useTheme = function () { return React.useContext(exports.ThemeContext); };
exports.ThemeProvider = function (props) {
    return jsx(exports.ThemeContext.Provider, { value: props.theme, children: props.children });
};

var Icon = function (_a) {
    var name = _a.name, color = _a.color, rest = __rest(_a, ["name", "color"]);
    var largest = Math.max.apply(Math, [12, 16, 24]);
    return jsx("svg", __assign({ width: largest, height: largest, "data-icon": name }, rest, {
        children: jsx("path", { fill: color || "currentColor" })
    }));
};
exports.Icon = Icon;

var Toolbar = function (props) {
    var theme = exports.useTheme();
    var count = shim_1.useSyncExternalStore(store_1.counterStore.subscribe, function () {
        return store_1.counterStore.getSnapshot().count;
    });
    var label = with_selector_1.useSyncExternalStoreWithSelector(
        store_1.counterStore.subscribe,
        store_1.counterStore.getSnapshot,
        store_1.counterStore.getSnapshot,
        function (state) { return state.label; }
    );
    var kind = Object.prototype.toString.call(props.items);
    var items = Array.prototype.slice.call(props.items, 1);
    return jsx("div", {
        className: "toolbar " + theme,
        "data-kind": kind,
        children: [
            jsx("span", { children: [label, ": ", count] }),
            items.map(function (item) { return jsx(Icon, { name: item, color: "red" }, item); })
        ]
    });
};
exports.Toolbar = Toolbar;
Object.assign(exports, { Badge: function (props) { return jsx("b", { children: props.children }); } });
if (process.env.NODE_ENV !== "production") {
    Toolbar.displayName = "DevToolbar";
} else {
    Toolbar.displayName = "Toolbar";
}
