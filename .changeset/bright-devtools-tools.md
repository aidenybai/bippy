---
"bippy": patch
---

Add target-aware instrumentation, stable reverse Fiber ID lookup, React 16 compatibility, standalone hook inspection, DevTools root tracking, and columnless and inline-require source-map hook-name parsing.
